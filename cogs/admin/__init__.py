"""
Module `admin` contains admin commands, and UI components
the be used within admin commands.

This module's __init__.py file does not contain any imports,
because we don't need to use it anywhere else.
"""
from .admin_cog import AdminCog
