"""
Module `commands` contains all of the cogs/groups that the
bot will use. These commands either interact with the discord
API directly, or through `components.abc.View` instances.
"""
from admin import *